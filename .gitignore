node_modules
dist
dist-ssr
.DS_Store
*.local
.vite

# Generated from tokens/tokens.json by `bun run tokens`, which both `dev` and
# `build` run first. Ignoring it also keeps Tailwind's content detection from
# scanning it and mistaking semantic token names for utility class names.
src/tokens.generated.css

# Local secrets
.env
.env.*
!.env.example

# rig writes timestamped backups next to the config it rewrites
rig.yaml.rig-bak-*

# local agent caches
.serena/
.haft/
